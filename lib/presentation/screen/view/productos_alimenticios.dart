import 'package:flutter/material.dart';

class ProductosAlimenticios extends StatelessWidget {
  const ProductosAlimenticios({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.white,
        title: const Text(
            'Elige tus Alimentos'), // Título de la barra de aplicación
      ),
      body: const Column(
        children: [],
      ),
    );
  }
}
